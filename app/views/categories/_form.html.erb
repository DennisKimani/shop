<%= render 'layouts/errors', :object => @category %>

<%= form_for @category, :html => {:multipart => true} do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.file_field :logo %>
<%= f.submit %>
<% end %>
