<%= render 'layouts/errors', :object => @sacco %>

<%= form_for @sacco, :html => {:multipart => true} do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.file_field :logo %>
<%= f.submit %>
<% end %>
